import { defineStore } from "pinia";

export const useDictionaryStore = defineStore("dictionary", {
    ///state
    state() {
        return {
            word : 'Dictionary',
        }
    }
    //methods

    //getters
});